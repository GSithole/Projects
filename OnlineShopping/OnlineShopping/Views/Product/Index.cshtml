@model IEnumerable<DataConection.Product>
@using OnlineShopping.CustomHtmlHelpers;
@{
    ViewBag.Title = "Index";
}


<h2>Index</h2>

<div class=" container">
    <div class="row">
        @foreach (var item in Model)
        {


            <div class="col-md-4">

                @*@Html.DisplayFor(modelItem => item.ProdID)*@
                <ul>
                    <li> @Html.DisplayFor(modelItem => item.ProdName)</li>

                    <li> @Html.Image(item.ImageUrl, "image", new { @class = "img-size" }) </li>

                    <li> R @Html.DisplayFor(modelItem => item.Price) </li>

                    <li> @Html.ActionLink("View produnct", "AddOrCreate", new { @id = item.ProdID, @userid = User.Identity.Name }) </li>
                </ul>
            </div>


        }
    </div>
</div>


